# Database Configuration
# Required: PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# 
# Note: If your password contains special characters (like < > ( ) { } [ ] \ ^ ` | ~),
# the connection code will automatically URL-encode them for you.
#
# Example for local development:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/leaderboard_db
#
# Example for hosted PostgreSQL (e.g., Supabase, AWS RDS, etc.):
# DATABASE_URL=postgresql://user:password@host.example.com:5432/dbname?sslmode=require
#
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

